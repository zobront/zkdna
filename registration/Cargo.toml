[package]
name = "merkle"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
dna_merkle_lib = { path = "../dna_merkle_lib" }
hex = "0.4"
